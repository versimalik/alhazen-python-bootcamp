#RANGE() FUNCTION
# returns a sequence of numbers
# starting from 0 (by default)
# and increments by 1 (by default)
# and stop before a specified number
# for integer
# range(start, stop before, increments)

numbers = range(1, 11)
print(numbers)
print(list(numbers))